# Base Tokens

## Summary

Base tokens are normal tokens that do not represent a DeFi position.  Base tokens are underlying token of a [DeFi Position Token](./defi-tokens.mdx) and can be used to create DeFi positions.

## Endpoints

### GET [/api/v1/baseTokens](todo)
This endpoint fetches all base tokens that are possible through Enso.